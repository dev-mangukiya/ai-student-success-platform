# Copy this file to `.env` and fill in real values. Never commit `.env`.

# Gemini API key for the AI Student Advisor (RAG pipeline) — added in Phase 6.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override default MLflow tracking URI (defaults to local ./mlruns).
MLFLOW_TRACKING_URI=./mlruns
